# 代码审计报告 — 2026-05-10

## 发现的问题

### 🔴 Bug 1: Qwen2 缺少 LAST_QUERY_STATES 保存
- `modify_qwen2.py` 的 forward 没有保存 query states
- `kv_cache.py` 的注意力加权只从 `modify_llama` 导入 `LAST_QUERY_STATES`
- Qwen2 上 Q·K^T 注意力加权完全失效（静默降级为纯 ℓ₁）

### 🟡 不一致 2: _to_legacy / _to_legacy_cache 实现不一致
- `sketching/kv_cache.py` 的 `_to_legacy` 缺少 `_key_cache` 回退
- `cache_baselines.py` 的 `_to_legacy_cache` 更完整
- 当前不影响（测试版本都走得通第一条路径），但为未来安全应统一

### 🟡 不一致 3: pos_shift 文件归档
- `modify_qwen2.py` 的正向替换模式和其他三个文件一致，OK
- 四个文件都各自复制了 `_resolve_past_kv_layer` 和 `apply_rotary_pos_emb_single`
- 代码重复 ~40 行 × 4 = 160 行，可提取为共享模块但不影响功能

### ⚪ 清理项 4: data_sources.py 冗余变量
- line 85-86: `question_tokens_bare` 和 `needle_tokens_bare` 定义后未使用

## 修复计划
1. modify_qwen2.py 加入 LAST_QUERY_STATES 保存
2. kv_cache.py 的 Q·K^T 导入同时尝试 modify_llama 和 modify_qwen2
3. data_sources.py 删除未使用变量
