runtime:
  backend: torch
  device: cpu
  seed: 42
  deterministic: true
  prefill_chunk_size: 64
  attention_prefill_chunk_size: 8
  local_files_only: true
  fail_on_error: true
  resume: true
  warmup_steps: 1
