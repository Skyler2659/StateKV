# L1-Robust KV Cache：工作摘要 — 2026-05-09

> **项目**：StreamingLLM + Sketching — ℓ₁-Robust KV Cache
> **今日工作**：GPU 迁移、模型扩展、注意力加权评分、代码重构
> **作者**：Skyler Wang

---

## 1. 架构与代码变更

### 1.1 pos_shift 重写

将基于 hook 的 RoPE 拦截方案（跨 HF 版本脆弱）替换为全量前向替换策略（与 `modify_gpt_neox.py` 相同架构）：

| 方案 | 状态 | 原因 |
|------|:--:|------|
| Hook（`_dispatch_apply_rotary`） | ❌ 放弃 | HF ≥4.45 中 cos/sin 表仅覆盖新 token 位置 |
| 全量前向替换 | ✅ 采用 | 自生成全缓存 cos/sin；所有版本可用 |

适配 transformers 4.46.3 新增的关键组件：

- `_resolve_past_kv_layer`：处理 DynamicCache 整体传入 attention 层（HF 4.50+）
- 基于 config 的 polyfill：`num_heads`、`head_dim` 等从 `self.config` 读取
- 缓存返回格式：输入为 DynamicCache 时返回 `DynamicCache.update()` 结果
- `rotary_emb` try/except：优先 `position_ids=`，回退 `seq_len=`

### 1.2 注意力加权 L1 评分（实验性）

添加 `LAST_QUERY_STATES` 共享存储于 `modify_llama.py` 与 `kv_cache.py` 之间：

```
L1RobustKVCache._select_indices_for_layer：
  ℓ₁_score ← ‖V·R⁻¹‖₁                     （结构重要性）
  attn_w   ← softmax(Q_last·V^T / √d)      （查询相关性）
  final    ← ℓ₁_score × attn_w              （组合分数）
```

**发现**：Q·V^T 作为注意力代理在隐形针测试中效果较弱（针与干草堆共享词汇时无法产生
足够的 Q-V 方向对比度）。结果与纯 ℓ₁ 持平。保留在代码库中作为未来实验性标志。

### 1.3 代码重构

将 672 行的 `benchmark.py` 拆分为三个文件：

| 文件 | 行数 | 职责 |
|------|:--:|------|
| `benchmark.py` | 240 | 主协调器、评估循环、策略调度 |
| `data_sources.py` | 90 | 四个文本构建器（repeat、wikitext、long、needle） |
| `cache_baselines.py` | 95 | SlidingWindowKVCache + DynamicCache 转换器 |

用 `COMPARISON_SPEC` 配置表替换了 150 行重复的 `run_decode_eval` 调用：

```python
COMPARISON_SPEC = {
    "full":   ["plain", "sketching", "main", "sliding_window"],
    "three":  ["recency_only", "sink_l1_last", "sink_recent_l1_last"],
    "needle": ["recency_only", "main", "l1_mixed"],
}
```

### 1.4 DynamicCache 4.51+ 兼容性

在所有 transformers 版本中添加了多层回退的缓存格式转换：

```
_to_legacy_cache：
  尝试：to_legacy_cache()              # HF 4.45-4.50
  尝试：layers[i].keys / .values       # HF 4.51+
  尝试：key_cache / _key_cache         # HF 4.45-4.50 回退
```

### 1.5 新基准数据源

添加了 `--text_source long`，使用 `wikitext-103-raw-v1` 进行标准长文档 PPL 评估。

---

## 2. 实验结果

### 2.1 平台

| | 本地 | Colab GPU |
|------|------|------|
| 模型 | pythia-410M-deduped | TinyLlama-1.1B-Chat / PY007-TinyLlama-1.1B-base |
| GPU | CPU | T4 16GB |
| transformers | 4.33.0 | 4.46.3 |
| pos_shift | ✅ GPT-NeoX 前向替换 | ✅ Llama 全量前向替换 |

### 2.2 大海捞针 — TinyLlama Chat（基线）

```
needle_pos  recency_only   main    l1_mixed    l1 vs main
   200        27.8        24.4      19.3      l1 赢 21%
   400        31.3        26.3      24.7      l1 赢 6%
   800        29.9        27.5      29.7      main 赢 8%
```

**发现**：ℓ₁ 在中短检索距离上优于 StreamingLLM。性能随距离增加而衰减，原因是*候选池稀释效应*：随着干草堆增大，结构相似的干扰 token 与针竞争 ℓ₁ 淘汰预算。

### 2.3 大海捞针 — TinyLlama Base

```
needle_pos  recency_only   main    l1_mixed    l1 vs main
   200        27.8        24.4      22.9      l1 赢 6%
   400        31.3        26.3      23.4      l1 赢 11%
   800        29.9        27.5      27.6      持平
```

**发现**：Base 模型呈现相似模式——中等距离 ℓ₁ 有优势，极端距离稀释。Chat 模型的指令微调可能放大了注意力汇聚效应，为 ℓ₁ 提供了更大的对比空间。

### 2.4 注意力加权评分结果

Q·V^T 注意力加权在所有三个针距离上测试。结果与纯 ℓ₁ 持平（在噪声范围内）。隐形针（与干草堆词汇相同）无法为此代理产生足够的 Q-V 方向对比度。

### 2.5 网格搜索（pythia-410M，CPU，前日数据）

```
mixed_recent_keep   ℓ₁ 预算   PPL (wikitext)
      32              91       24.31
      48              75       23.32
      64              59       22.67
      80              43       22.21 *  ← 追平 StreamingLLM (22.01)
      96              27       22.25
```

---

## 3. 遇到的问题与解决方案

| # | 问题 | 根因 | 解决方案 |
|---|------|------|---------|
| 1 | `past_key_values` 意外 kwarg | HF 4.46 修改了 attn forward 签名 | 添加 `**kwargs` |
| 2 | `num_heads` 不存在 | HF 将 `num_heads` → `num_attention_heads` | 基于 config 的 polyfill |
| 3 | `DynamicCache` 不支持下标 | HF 4.51 移除了 `__getitem__` | 使用 `.layers` API |
| 4 | `to_legacy_cache()` 缺失 | HF 4.51 移除了方法 | 直接访问 `.layers[i].keys/.values` |
| 5 | RoPE 中索引越界 | 原生 cos/sin 太小（仅新 token） | 自生成全缓存 cos/sin |
| 6 | CUDA cublas 错误 | cos/sin 以 int64 生成（dtype bug） | 传递 float dummy tensor 给 `rotary_emb` |
| 7 | DynamicCache 返回格式 | 前向返回 tuple，模型期望 Cache | `update()` 并返回输入缓存对象 |
| 8 | Colab 内存不足 (3B) | FP32 模型超过 12GB RAM | FP16 模型加载 |
| 9 | PG19 数据集脚本不支持 | HF datasets v3 移除脚本支持 | 切换至 wikitext-103 |

---

## 4. 当前局限性

1. **模型规模**：所有 GPU 结果均在 1.1B 模型上。3B+ 需要 FP16 优化。
2. **基准覆盖**：仅有自制 needle + wikitext PPL。缺失 LongBench、RULER、PG19。
3. **注意力代理**：Q·V^T 对隐形针太弱。需要真正的 Q·K^T。
4. **候选池稀释**：ℓ₁ 优势随检索距离衰减。
5. **transformers 版本敏感性**：≥4.45 版本上 pos_shift 需要全量前向替换，需随 HF 演进维护。

---

## 5. 下一步计划

### 5.1 即刻

- 切换至 OpenLlama-3B（无需审批）+ FP16
- 在 1.1B base 模型上运行 `--text_source long` 获取标准 PPL 基线
- 在 3B 上运行 needle 200/400/800 测试规模-稀释假设

### 5.2 短期（暑期科研前）

- 安装 LongBench 评估工具并在 3B 模型上运行
- 添加 H2O 基线实现
- 运行完整消融：sketch_dim、recompute_interval、per_layer vs shared

### 5.3 暑期科研（与 Woodruff 合作）

- 用真正的 Q·K^T 注意力权重替换 Q·V^T 代理
- 推导 ℓ₁-attention 组合评分的理论边界
- 在合适的 GPU 硬件上扩展至 7B 模型

---

*作者：Skyler Wang*
