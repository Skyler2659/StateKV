# L1-Robust KV Cache：工作摘要 — 2026-05-10

> **作者**：Skyler Wang
> **今日重点**：GPU 服务器部署、7B 模型适配、多 benchmark 扩展、代码审计与清理

---

## 1. 基础设施

### 1.1 AutoDL RTX 4090 部署

- GPU：NVIDIA RTX 4090 24GB
- Python 3.12 + PyTorch 2.3.0 + CUDA 12.1 + transformers 4.46.3
- VS Code Remote SSH 连接
- GitHub SSH 密钥配置完成
- HF 镜像 `hf-mirror.com` 代理下载
- 缓存目录迁移至数据盘 `/root/autodl-tmp/.cache/huggingface`

### 1.2 模型矩阵

| 模型 | 参数量 | 架构 | 上下文 | 审批 | 状态 |
|------|:--:|------|:--:|:--:|:--:|
| pythia-410M | 410M | GPT-NeoX | 2048 | ✅ | 本地跑通全部基准 |
| TinyLlama-1.1B | 1.1B | Llama | 2048 | ✅ | GPU 跑通全部基准 |
| huggyllama-7B | 7B | Llama-1 | 2048 | ✅ | GPU PPL 网格跑通 |
| Llama-2-7B | 7B | Llama-2 | 4096 | ✅ | 已下载、待测试 |
| Qwen2.5-7B | 7B | Qwen2 | 32K | ✅ | pos_shift 已适配、待测试 |

---

## 2. 代码适配与修复

### 2.1 7B FP16 全链路适配

| 问题 | 根因 | 修复 |
|------|------|------|
| `linalg.qr` 不支持 FP16 | CUDA `geqrf` 算子缺 Half 支持 | `update_basis` 中 `sv.float()` 强制升 FP32 |
| Exp(1) 样本产生 NaN | FP16 下 `1e-8` clamp 退化为下溢 | `_safe_exp_samples` 内部强制 float32 |
| R 矩阵奇异（对角归零） | 129×128 矩阵极度病态 | 自适应 jitter + 1e-2 兜底重试 |
| NaN 检测与退路 | R 含 NaN → inv 永远失败 | `r_inv=None` 退回到纯 ℓ₁ 范数 |
| QR 16384×128 耗时过长 | 7B head_dim=128 → sketch_dim=16384 | 上限缩至 4096（精确 ℓ₁ 路径不受影响） |
| 模型 FP32 加载 OOM | 默认加载 26GB FP32 | `torch_dtype=float16` 自动启用 |

### 2.2 pos_shift 兼容性

- Llama：全量 forward 替换 + config polyfill + DynamicCache 适配
- GPT-NeoX：同架构，config polyfill + `_resolve_past_kv_layer`
- Falcon：config polyfill + `_resolve_past_kv_layer`
- **Qwen2（新增）**：`modify_qwen2.py` — 复制 Llama 架构，`Qwen2Attention` → 替换 forward

### 2.3 Q·K^T 注意力加权修复

- `kv_cache.py` 中 `Q·V^T` 改为 `Q·K^T`（真注意力内积）
- Qwen2 缺少 `LAST_QUERY_STATES` 保存 → 已补加
- kv_cache.py 同时从 `modify_llama` 和 `modify_qwen2` 读取 query states

### 2.4 代码审计清理

- `data_sources.py` 删除未使用的 `question_tokens_bare`、`needle_tokens_bare` 变量
- `l1_sketch.py` 注释更新
- `_to_legacy` / `_back_to_original` 统一三层回退逻辑

---

## 3. 实验进展

### 3.1 7B 标准 PPL（wikitext-103 长文）

```
mixed_recent_keep   recency_only   sink_l1_last   l1_mixed     vs main(8.37)
      32               256.4         441.4         13.56        +62%
      48               256.4         441.4         10.73        +28%
      64               256.4         441.4          9.58        +14%
      80               256.4         441.4          8.98         +7%
      96               256.4         441.4          8.59         +3%
```

结论：7B 上 ℓ₁+recency 混合策略追平 main（最近 = 8.59 vs 8.37，差 2.6%），与 1.1B 结论一致。

### 3.2 7B 标准针测试（needle_std）

- 老模型（huggyllama 2048 上下文）上针测试上下文不够，三组 PPL 均在 ~50（噪声）
- 深度=1.0 时全策略 PPL~1.1（都能看到针）
- 换 Llama-2 4096 上下文后需重跑

### 3.3 NarrativeQA（1.1B）

```
sample=0:  recency=14.9, main=17.5, l1_rk32=15.9  ← ℓ₁ 小赢（-9% vs main）
sample=0:  l1_rk0=55.7                             ← 纯 ℓ₁ 炸（无 recency 保底）
sample=2:  recency=7.9,  main=8.5,  l1_rk32=11.3  ← ℓ₁ 输了
```

结论：NarrativeQA 多数样本答案可从问题文本推断，不依赖文档远距离事实 → ℓ₁ 无区分度。

---

## 4. Benchmark 生态

### 4.1 当前支持的数据源

| `--text_source` | 功能 | 添加日期 |
|------|------|:--:|
| `repeat` | 重复文本 PPL | 5/8 |
| `wikitext` | wikitext-2 PPL | 5/8 |
| `long` | wikitext-103 长文 PPL | 5/9 |
| `needle` | 隐形针（同词汇） | 5/8 |
| `needle_std` | 标准 NIAH（OOV 针） | 5/10 |
| `narrativeqa` | NarrativeQA 长文问答 | 5/10 |
| `hotpotqa` | HotpotQA 多跳推理 | 5/10 |

### 4.2 对比模式

| `--comparison_mode` | 策略 | 新增日期 |
|------|------|:--:|
| `needle` | recency_only + main + l1_mixed | 5/8 |
| `three` | recency + sink_l1 + sink_recent_l1 | 5/8 |
| `full` | plain + sketching + main + sliding | 5/8 |
| `grid` | recency_only + main(各一次) + l1 × N 个 RK | **5/10 新增** |

---

## 5. 下一阶段

1. **Llama-2-7B**：4096 上下文，跑标准 NIAH 全深度 + HotpotQA 全样本
2. **Qwen2.5-7B**：32K 上下文，终极长上下文测试
3. **HotpotQA 完整评估**：5 样本 × 5 RK 网格（已就绪，一个命令）
4. **论文写作**：1-pager 草稿整合所有数据

---

*作者：Skyler Wang*
