# ℓ₁-Robust KV Cache 改进方向

> 当前状态：ℓ₁ 杠杆分数在短-中距离检索上显著优于 StreamingLLM（21%），但在远距离受候选池稀释影响退化为持平。以下按"改动量/预期收益"排序，前 10 条今年暑期可做，后 10 条为长期方向。

---

## 一、轻量优化（改动小、风险低、立刻跑）

### 1. 自适应混合预算

当前 `recent_keep` 是固定值（如 80）。改为根据序列位置动态调整：

```
早阶段（token < 512）：     recent_keep 小（多用 ℓ₁ 探索历史）
晚阶段（token ≥ 512）：     recent_keep 大（保留当前上下文）
```

直觉：刚启动时注意力汇聚效应还没建立，ℓ₁ 的结构信号更有用；对话深入后 recency 权重应上升。

**改动**：`_select_with_recency_mix` 中加入 `min(80, self._steps // 4)` 的动态预算。

### 2. 指数衰减 ℓ₁ 分数

当前 ℓ₁ 分数是无衰减的。加上时间衰减：

$$s_i' = s_i \cdot \exp(-\lambda \cdot (n - i))$$

其中 $\lambda$ 为衰减率。这等价于在 V 空间结构信号上叠加了一个温和的 recency bias，可能缓解候选池稀释。

**改动**：`_select_indices_for_layer` 中 `scores = estimator.scores(...)` 后乘 `exp(-λ * distances)`。

### 3. 分层缓存预算

不同层对远距离 token 的依赖不同。浅层可能更需要局部上下文，深层更能利用长程依赖：

```
layer 0-7:    recent_keep 大（浅层偏局部）
layer 8-15:   recent_keep 中
layer 16-23:  recent_keep 小（深层用 ℓ₁ 抓长程）
```

或者反向——取决于模型的注意力模式。两种都测，看哪个更好。

**改动**：`L1RobustKVCache` 中维护 `layer_recent_keep` 列表，按层索引取不同值。

### 4. 结构化 CountSketch

当前 CountSketch 每列随机映射到任意桶。改用分块 CountSketch：将序列切成连续块，块内哈希到同一桶。

直觉：连续 token 往往语义相关，分块可以保持局部结构的同时实现压缩。

**改动**：`CountSketch._ensure_capacity` 中 `hash_buckets = i // block_size`。

### 5. 预热期跳过重构

`refit_interval=32` 意味着每 32 步触发一次 QR 分解。但前 128 步（缓存未满）根本不需要淘汰——此时可以完全跳过 sketch 更新。

**改动**：`_select_indices_for_layer` 中若 `seq_len <= cache_size` 则跳过所有 estimator 操作。

---

## 二、中等改动（需要重新设计部分组件）

### 6. 真正的 Q·K^T 注意力加权

当前用的是 Q·V^T 代理，太弱。改为在 `__call__` 淘汰前，用 `torch.matmul(Q, K^T)` 算出真正的注意力权重，然后：

$$s_i' = s_i \cdot \text{softmax}(Q_{last} \cdot K^T / \sqrt{d})_i$$

Q 和 K 都经过 RoPE，包含完整位置编码——比 Q·V^T 信息量高得多。

**改动**：pos_shift 中同时存 K_last（或直接存 `attn_weights`），kv_cache 中读取并乘到 ℓ₁ 分数上。

### 7. 增量杠杆分数追踪

当前每 32 步全量重建 R⁻¹。改为增量更新：

- 每个新 token 到来时，用 CountSketch 增量更新 SV 的行
- 用 Woodbury 公式增量更新 R⁻¹（O(d²) 而非 O(d³)）
- 只在必要时（累积误差过大）才全量重建

**改动**：`CountSketch` 加 `apply_incremental`，`L1LeverageScoreEstimator` 加 `update_basis_incremental`。

### 8. 多尺度时间窗口

不同距离的 token 用不同的淘汰策略：

```
窗口 1 (0-128)：    纯 recency（全保留）
窗口 2 (129-512)：   ℓ₁ 50% + recent 50%
窗口 3 (>512)：      纯 ℓ₁（无 recency bias）
```

每个窗口有独立的 `cache_budget` 和 `recent_keep` 比例。

**改动**：`_select_with_recency_mix` 改为多窗口选择。

### 9. 基于注意力的提前筛选

在 ℓ₁ 打分之前，先用上一层的 attention weight 做一次粗筛——过滤掉 attention 几乎为 0 的 token，只对剩下的做 ℓ₁ 打分。

直觉：如果上一层完全不关注某个 token，它对当前层的 V 空间贡献大概率也低。

**改动**：`_select_indices_for_layer` 入口处读上一层的 `attn_weights`，`scores = estimator.scores(v_rows[surviving])`。

### 10. 联合 K-V 评分

当前只看 V。扩展为 $(K, V)$ 联合矩阵：

$$V' = [V \| K] \in \mathbb{R}^{n \times 2d}$$

在拼接后的矩阵上做 ℓ₁ 杠杆分数估计。

直觉：K 包含了 "这个 token 在查询时的表现"，V 包含了 "这个 token 在被注意时的贡献"。两者合并应该比单看 V 更全面。

**改动**：`_get_layer_v_rows` 改为同时返回 K 和 V，拼接后传给 estimator。

---

## 三、重度改动（需要有理论支撑，适合与 Woodruff 合作）三重度

### 11. KV-Cache 专用 Sketching 矩阵

当前 CountSketch 是通用的。能否利用 transformer 的两个特性设计更高效的 sketching？

- **因果性**：token $i$ 只能看到 token $< i$ → sketch 可以设计为上三角结构
- **指数衰减**：注意力权重随距离指数衰减 → 对角附近的元素可以有更高的采样概率

目标：保证 $r = O(d \log n)$（当前是 $O(d^2)$）。

### 12. ℓ₁-Attention 联合理论

推导一个 bound：在 ℓ₁ 杠杆分数 × attention 权重的联合评分下，被淘汰 token 对注意力输出的最大扰动上界。

$$\\|\text{output}(q) - \text{output}_S(q)\\| \\leq f(B, d, \\varepsilon)$$

如果 $f$ 随预算 $B$ 指数衰减且衰减速度快于纯 ℓ₁ 或纯 attention 方案，这就是一篇 NeurIPS 理论 paper。

### 13. 在线 Learning-to-Sketch

用少量标注数据学习每个注意力层的"最优"哈希函数，替代随机哈希。

**改动**：`CountSketch` 的 `hash_buckets` 从随机 → 可学习参数，用 Gumbel-Softmax 或 RL 做离散优化。

### 14. 对抗性 Token 检测

ℓ₁ 杠杆分数的物理含义——高杠杆 token 是那些"不能被其余 token 线性表示"的点。这在对抗性场景中可能特别有用：

- 检测 prompt injection（注入的越狱指令 V 向量通常是离群的）
- 识别 hallucination 的关键诱发 token

### 15. 蒸馏式压缩

用一个小型代理模型（100M 参数）的 ℓ₁ 分数来指导大模型（7B）的 KV 缓存淘汰。

直觉：ℓ₁ 杠杆分数是 V 空间的几何性质，与模型大小关系不大。小模型估算分数，大模型使用结果——零额外推理开销。

### 16. 跨层一致性约束

当前每层独立淘汰。但不同层删除不同 token 会导致跨层信息不一致——第 10 层还在的一个 token 可能在第 20 层被删了。

给一个跨层一致性的约束：所有层共享同一个 token 保留集合，但使用加权投票（层越深权重越大）。

### 17. 长尾 token 保护

ℓ₁ 杠杆分数可能偏向高频模式，忽略长尾但关键的信息（如数字、专有名词）。加一个"信息密度"修正项：

$$s_i' = s_i \cdot (1 + \gamma \cdot \text{entropy}(token_i))$$

高熵 token（稀有词、数字）获得额外保护。

### 18. 基于聚类的预算分配

在淘汰前对 V 矩阵做快速 k-means（k = budget），每类至少保留一个代表 token，剩下的预算按 ℓ₁ 分配。

直觉：保证语义多样性——不同"话题"至少有一个 token 留存。

### 19. 滑动锚点策略

不固定保留前 k 个 token 作为 sink。而是每 B/k 步选一个新的"锚点"（当前 ℓ₁ 分数最高的 token），把它作为新的 sink 保留。

直觉：注意力汇聚点可能会随时间"漂移"，固定的前 k 个 token 不一定永远是最佳汇聚点。

### 20. 预填充 Token 级别的淘汰预算

当前 prefill 和 decode 用同一套淘汰逻辑。但 prefill 阶段一次涌入大量 token，如果一开始就淘汰，可能会错失早期的关键 token。

改为两阶段策略：
- Prefill：不淘汰，全量保留（或只做粗粒度压缩）
- Decode：启动精细的 ℓ₁ 淘汰

---

## 优先级矩阵

```
                    预期收益 →
改动量 ↓          低           中           高
─────────────────────────────────────────────────
小               #5 跳过     #1 自适应    #2 指数衰减
                 #4 分块     #3 分层预算

中               #9 预筛选   #6 真Q·K^T   #7 增量更新
                 #8 多窗口   #10 联合K-V

大               #14 对抗    #12 联合理论  #11 专用Sketch
                 #19 锚点    #16 跨层约束  #13 学习Hash
```

---

*建议：暑期科研前做 #1-#5（快出数据），暑期中和 Woodruff 讨论后做 #6-#12 中的 2-3 项。*
