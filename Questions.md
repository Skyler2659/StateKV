# 审稿人提问 — L1-Robust KV Cache

> 这些问题相当于 NeurIPS/ICML 审稿人或者 David Woodruff 会当面问你的。
> 如果全部能答上来，你就真正掌握了这个项目。

---

## §1 — 数学基础

**Q1.** 请解释为什么 CountSketch 本身只能给出 ℓ₂ 子空间嵌入，加上指数随机变量 $1/E_i$ 后就变成了 ℓ₁ 子空间嵌入。Woodruff 定理 38 的核心直觉是什么？用一句话概括。

**Q2.** ℓ₁ 杠杆分数定义为 $\|(V \cdot R^{-1})_{i,*}\|_1$。为什么 $R$ 是从 $S \cdot V$（草图矩阵）的 QR 分解中获取的，而不是直接从 $V$ 本身的 QR 分解中获取？$S$ 的什么性质保证了 $V \cdot R^{-1}$ 是一个良条件基？

**Q3.** 你的实现中 `sketch_dim` 设为 $O(d^2)$，其中 $d$ 是 head 维度。对于 pythia-410M 来说 $d=64$，即 $r \approx 4096$。如果你把 $r$ 设为 256 会怎样？什么理论保证会被打破？在实验中会观察到什么具体的失效模式？

**Q4.** 你声称 ℓ₁ 杠杆分数在 token 选择上比 ℓ₂ 杠杆分数更鲁棒。请构造一个具体的数值例子（5 个 token、head_dim=2），使得在相同预算下 ℓ₂ 选中了一组（更差的）token 而 ℓ₁ 选中了另一组。给出完整的数学推导。

**Q5.** 你的 `L1LeverageScoreEstimator` 通过 $V \cdot R^{-1}$ 计算**精确**分数，而不是用 Cauchy 分布的随机投影。精确计算和随机投影的计算成本分别是什么？当 head 维度 $d$ 达到多少时随机投影会变得更便宜？随机投影引入的是偏差、方差，还是两者都有？

---

## §2 — 算法设计

**Q6.** 为什么你的混合预算策略需要同时保留汇聚 token（sink）和最近 token（recency）？ℓ₁ 分数难道不应该天然给初始 token（注意力汇聚点）和最近 token 打高分吗？在什么情况下 ℓ₁ 会**无法**自动保留注意力汇聚 token，必须靠显式预留？

**Q7.** 你的 `recompute_interval` 默认是 32。如果设为 1（每步都重建）与 512（几乎从不重建）分别会怎样？$R^{-1}$ 矩阵变陈旧时具体的失效模式是什么？

**Q8.** 在 `_select_with_recency_mix` 中，你始终保留**最后一个** token。如果不保留会怎样？ℓ₁ 分数会天然给最近的 token 打高分吗？请从 decode 步骤中 V 矩阵的结构来论证你的回答。

**Q9.** 你从跨注意力头**平均**后的 $V$ 计算 ℓ₁ 分数。为什么跨头平均是合理的？有没有可能在某个 head 的 V 空间中很重要的 token 在另一个 head 中不重要？如果做逐头选择，代价是什么？

**Q10.** 逐行解释 `CountSketch.apply()` 做了什么。`scatter_add_` 在干什么？为什么哈希桶和符号只生成一次然后缓存，而不是每次调用都重新生成？如果每次调用都重新生成会出什么问题？

---

## §3 — 实现与系统

**Q11.** pos_shift 为什么必须存在？在基于 RoPE 的 transformer 中，如果你淘汰了 KV 缓存中段的 token 却不修正位置编码，会发生什么具体的错误？用一个具体的位置编号例子说明。

**Q12.** 你的 `enable_llama_pos_shift_attention` **替换了整个** `LlamaAttention.forward`。这种做法的维护风险是什么？举出至少三个 HuggingFace 内部改动可能让你的代码崩溃的情况，并说明它们分别发生在哪个 transformers 版本中。

**Q13.** 在 `modify_llama.py` 中，你**自生成**覆盖完整 `kv_seq_len` 的 cos/sin 表，而不是使用原生 forward 生成的 cos/sin。为什么在 transformers 4.45+ 上这是必须的？这些版本中原生 cos/sin 的具体形状是什么？

**Q14.** `modify_llama.py` 中的 `LAST_QUERY_STATES` 字典是一个模块级全局变量。当你在同一个模型上**依次**运行三个 benchmark 策略时，第一个策略 forward 中保存的 Q 会不会泄漏到第二个策略的淘汰步骤中？请精确追踪时间线来论证你的回答。

**Q15.** 你的 `_to_legacy_cache` 有三层回退（`to_legacy_cache()` → `.layers` → `key_cache`）。在 transformers 4.46.3 上，实际命中哪一层？在 4.33.0 上命中哪一层？在最新 Colab 版本（4.51+）上命中哪一层？

---

## §4 — 实验方法

**Q16.** 为什么 `main`（StreamingLLM）在你所有的 needle 基准上都显著优于 `recency_only`（纯滑窗）？注意力汇聚 token 起作用的具体机制是什么？为什么当文本超过模型原生上下文窗口时这个机制更关键？

**Q17.** 你的 needle 基准显示 ℓ₁ 优势随距离衰减：21% → 6% → 持平。这是 ℓ₁ 评分的**根本局限**，还是在 128 token 预算下**任何**选择策略的局限？你如何设计一个实验来区分这两种解释？

**Q18.** 为什么你的 OOV 针测试给出了 250 倍优势，而隐形针测试只有 6-21%？这说明了 ℓ₁ 杠杆分数到底在度量什么？ℓ₁ 捕捉到的是**语义**重要性还是**分布**新颖性？

**Q19.** 你在 pythia-410M 和 TinyLlama-1.1B 上跑了实验，两者的 head_dim 都是 64。Llama-2-7B 的 head_dim 是 128。你预计当 head_dim 从 64 升到 128 时，ℓ₁ 优势会如何变化？**质量**（信号分离度）和**开销**（sketch_dim = O(d²)）都会变——请量化两者。

**Q20.** 你的网格搜索显示 `recent_keep=80` 最优（ppl=22.21 vs main=22.01）。但 `recent_keep=96`（几乎纯 recency）的 ppl=22.25。这 0.04 的差距统计显著吗？你如何设计一个严谨的测试来确定 ℓ₁ token 的**最小有效数量**？

---

## §5 — 对比与定位

**Q21.** H2O（NeurIPS 2023）用累积注意力分数做淘汰。StreamingLLM（2023）用位置。CurDKV（2025）用 ℓ₂ CUR 分解。你的方法用 ℓ₁ 杠杆分数。每种方法各用一句话说明其**独有的失效模式**是什么——别的方案不会在这个场景下翻车。ℓ₁ 在什么场景下**唯一**胜出？

**Q22.** 如果你只能展示一张"杀手图"作为论文的核心证据，你会选哪张图？图上三条线分别是什么？横纵坐标分别是什么？读者在 10 秒内应该得出什么结论？

**Q23.** 你的方法每 `recompute_interval` 步需要做一次 CountSketch + QR + R⁻¹。在 7B 模型、32 层的推理场景下，你的方法相比 StreamingLLM（仅做 tensor 切片）的**端到端耗时**多了多少？给出数量级估算。

**Q24.** 有一个敌对审稿人说："这不过是用一个启发式替换了另一个启发式——你仍然在做 top-k 选择，只是换了个打分函数。ℓ₁ 选择的 token 能保留注意力输出质量的**理论保证**在哪里？" 请用三句话为你的方法辩护。

---

## §6 — 未来工作与元问题

**Q25.**（Woodruff 提问）目前的方法使用 CountSketch 加指数权重构造 ℓ₁ 嵌入。但子空间嵌入定理要求 $r = O(d^2/\varepsilon^2)$。能否利用 transformer 注意力的**序列结构**，设计一个 KV 缓存专用的 sketching 矩阵，使得同样的保证只需 $r = O(d \log n)$？核心直觉是什么？

**Q26.** 你已经确定**候选池稀释**是远距离检索的主要失效模式。请提出三个**正交**的改进方向（不能是"用更大的模型"或"加注意力权重"），每个方向用一句话解释核心直觉。
